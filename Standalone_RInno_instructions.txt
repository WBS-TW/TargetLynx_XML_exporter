
create_app(
  app_name    = "ML_exporter", 
  app_dir     = "ML_XML_exporter",
  pkgs        = c("jsonlite", "shiny", "magrittr", "shinydashboard", "xml2", "tidyverse", "DT", "pheatmap", "writexl"),  # CRAN-like repo packages
  include_R   = TRUE,     # Download R and install it with your app, if necessary
  default_dir = "pf")     # Install app in to Program Files
